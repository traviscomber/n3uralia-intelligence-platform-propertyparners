# Legacy code cleanup

This branch adds a reproducible audit for obsolete artifacts, suspicious filenames, potentially orphaned source files, unused dependency candidates, and legacy or placeholder content signals.

Deletion requires manual confirmation plus lint, model verification, build, and deployment evidence.
